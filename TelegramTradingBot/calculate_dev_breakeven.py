# here we will approximate the price at which the dev breaks even
# dev percentage holding 100/dev % holdings  = amount of x it needs to do to break even

# initial sol price pooled sol /supply

# initial sol price x amount fo x for ev to break even
